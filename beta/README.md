# Beta Staging

This folder is the public beta staging area for pre-release R8Radio downloads.

Use it for:

- beta installer builds
- beta `R8Radio-onedir.zip` builds
- temporary beta notes when needed

Simple rule:

- `beta/` is for invited testing before a full public release
- full public release artifacts still belong on GitHub Releases
